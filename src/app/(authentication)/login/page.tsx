import Login from "@/components/Authentication/Login";
import React from "react";

const page = () => {
  return (
    <div className="min-w-full">
      <Login />
    </div>
  );
};

export default page;
